add_executable(vector_of_ivec vector_of_ivec.c)
add_executable(hsi_demo hsi_demo.c)
add_executable(queue_demo queue_demo.c)
foreach (p vector_of_ivec hsi_demo queue_demo)
  target_link_libraries(${p} cgraph)
endforeach()